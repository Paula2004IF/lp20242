#COMANDO DEF E RETURN COM PRINT

def area (largura,altura):
    return largura*altura

def volume (largura, profundidade, altura):
    return area(largura,profundidade) * altura

print(area(2,3))
print(volume(2,3,5))   